[Unit]
Description=Program to automate control of docker containers
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/start-container %i
ExecStop=/usr/bin/stop-container %i

[Install]
WantedBy=multi-user.target